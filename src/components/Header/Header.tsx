import './Header.scss'

export const Header:React.FC= () => {
    return (
             <header className="content_header">
                <nav>
                    
                </nav>
             </header>
    );
  }
  